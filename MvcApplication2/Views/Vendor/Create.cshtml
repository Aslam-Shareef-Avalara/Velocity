@model  DataService.InventoryManagement.Vendor

@{
    ViewBag.Title = "Create"; 
  Layout = "~/Views/Shared/_InvLayoutPage.cshtml";
}

<h2>Create</h2>
<script>
    var selectedmenuid = "penav_invmgmt";
    $(document).ready(function () {
        //$("#createvendordiv").closest("form").validate();
    });
</script>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal" id="createvendordiv">
        <h4></h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.Partial("EditForm")
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
